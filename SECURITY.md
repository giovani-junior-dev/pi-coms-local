# Security

Pi extensions run with local system permissions. Review code before installing any Pi package.

This package provides local same-machine agent messaging. It stores local registry data under Pi's coms directory and uses OS-level local IPC.

Do not send secrets, credentials, private keys, or production PII through agent messages unless you understand and accept the risks.

To report a security issue, open a private GitHub security advisory or contact the repository owner.
